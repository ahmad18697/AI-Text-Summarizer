# AI Text Summarizer

A full-stack application that uses Hugging Face's BART model to summarize text.

## Features
- AI-powered text summarization
- History tracking
- Clean Material-UI interface

## Setup
1. Clone repository
2. Install dependencies:
   ```bash
   cd backend && npm install
   cd ../frontend && npm install

# Local development
 - contributer are always welcome